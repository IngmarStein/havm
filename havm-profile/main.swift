/// HAVM Connect exists solely to let Xcode generate a provisioning profile
/// for `ch.ingmar.havm`. Build once in Xcode (⌘B), then close the project —
/// the profile is picked up automatically by the havm build script.
///
/// This binary does nothing and exits immediately.

print("HAVM Connect — provisioning profile generated.")
